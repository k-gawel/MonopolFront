import { PropertyDirective } from './property.directive';

describe('PropertyDirective', () => {
  it('should create an instance', () => {
    const directive = new PropertyDirective();
    expect(directive).toBeTruthy();
  });
});
